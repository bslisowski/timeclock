import React from 'react';
import { Text, StyleSheet, View } from 'react-native';

const ClockinScreen = () => {
    return (
        <Text>Clockin Screen</Text>
    );
};

const styles = StyleSheet.create({

});

export default ClockinScreen;